overwrite: true
schema: 'graphql/schema.graphql'
documents: null
config:
  scalars:
    DateTime: string
generates:
  graphql/generated/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
